spring:
  datasource:
    url: jdbc:h2:mem:page_pulse_test;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false

confluence:
  base-url: http://localhost:8090/confluence
  username: testuser
  api-token: testtoken
