# Enable debug-level logging for Feign to see request/response details
logging:
  level:
    feign: DEBUG

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/page_pulse
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
